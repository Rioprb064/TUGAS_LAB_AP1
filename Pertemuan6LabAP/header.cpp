#include <iostream>//bawaan                             // Menyertakan pustaka standar untuk input/output
#include <cmath>//bawaan                                // Menyertakan pustaka standar untuk fungsi matematika seperti sqrt
#include "headerCustom.h"//header buatan sendiri        // Menyertakan header kustom buatan pengguna
u;                                                      // using namespace std;

i j(i a, i b) {                                // Fungsi jumlah: menambahkan dua bilangan bulat
    r a + b;                                   // Mengembalikan hasil penjumlahan
}

i k(i a, i b) {                                // Fungsi kurang: mengurangi dua bilangan bulat
    r a - b;                                   // Mengembalikan hasil pengurangan
}

awal() {                                       // Fungsi utama program menggunakan alias awal
    system("cls");                             // Membersihkan layar

    i n;                                       // Deklarasi variabel integer n

    o l j(2, 3) l e                            // Menampilkan hasil j(2, 3) diikuti baris baru
    o l k(5, 3) l e                            // Menampilkan hasil k(5, 3) diikuti baris baru

    d angka = 25.0;                            // Deklarasi variabel double angka
    d akar = a(angka);                         // Menghitung akar kuadrat angka dengan fungsi sqrt

    o l "Akar dari " l angka l " adalah " l akar l e  // Menampilkan hasil akar kuadrat
}